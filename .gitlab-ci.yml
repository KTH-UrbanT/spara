# We only need the stages test and deploy for this pipeline,
# as building is done during testing and deployment.
stages:
  - test
  - deploy

variables:
  COMPOSE_FILE: infra/docker-compose.test.yml
  REMOTE_USER: ubuntu
  REMOTE_HOST: $STAGING_HOST
  REMOTE_PATH: /home/ubuntu/spara/infra
  SSH_REMOTE_HOST_KEY: $SSH_STAGING_HOST_KEY
  SSH_GITHUB_PRIVATE_KEY: $SSH_MG_PRIVATE_KEY

# The 'container_setup' job initializes the Git repository, installs necessary dependencies,
# and prepares the environment for testing and deployment.
before_script:
  # install needed CLIs
  - apk add --no-cache git openssh-client rsync

  # prepare SSH dir and keys
  - mkdir -p ~/.ssh
  - echo "$SSH_GITHUB_PRIVATE_KEY" > ~/.ssh/github_key
  - chmod 600 ~/.ssh/github_key
  - echo "$SSH_REMOTE_HOST_KEY" > ~/.ssh/remote_key
  - chmod 600 ~/.ssh/remote_key

  # trust hosts
  - ssh-keyscan -H github.com >> ~/.ssh/known_hosts
  - ssh-keyscan -H $REMOTE_HOST >> ~/.ssh/known_hosts

  # fetch submodules using the GitHub key
  - |
    GIT_SSH_COMMAND="ssh -i ~/.ssh/github_key -o StrictHostKeyChecking=no" \
      git submodule update --init --recursive

  # inject .env files
  - echo "$MESSAGE_SERVICE_ENV" > message-service/.env
  - echo "$LLM_SERVICE_ENV"     > llm-service/.env

# The 'container_test' job builds and starts Docker containers using docker-compose,
# then runs pytest-based unit tests inside both the message-service and llm-service containers.
# It only runs for branches matching the pattern 'test/*'.
container_test:
  stage: test
  image: docker:24.0-cli
  services:
    - docker:dind
  script:
    - docker compose -f $COMPOSE_FILE build --no-cache
    - docker compose -f $COMPOSE_FILE up -d
    # Before running tests, we ensure that the test folders exist.
    - docker compose exec message-service bash -c "[ -d tests ] && PYTHONPATH=/message-service pytest tests/ || echo 'No tests directory found.'"
    - docker compose exec llm-service bash -c "[ -d tests ] && PYTHONPATH=/llm-service pytest tests/ || echo 'No tests directory found.'"
  rules:
    - if: '$CI_COMMIT_BRANCH =~ /^test\//'
      when: always

# Deploys the application to the staging environment.
# This function typically handles building the application,
# uploading artifacts, configuring environment variables,
# and triggering deployment processes to a staging server.
# It is used to test new features and changes in an environment
# that closely resembles production before releasing to end users.
deploy_staging:
  stage: deploy
  image: docker:latest
  environment:
    name: staging
    url: https://test.sparabot.com
  script:
    - echo "Deploying to staging server"

    # ensure the remote directory exists
    - ssh -i ~/.ssh/remote_key -o StrictHostKeyChecking=no $REMOTE_USER@$REMOTE_HOST "mkdir -p $(dirname $REMOTE_PATH)"

    # copy the infra/ folder to the remote
    - rsync -avz -e "ssh -i ~/.ssh/remote_key -o StrictHostKeyChecking=no" infra/ $REMOTE_USER@$REMOTE_HOST:$REMOTE_PATH/

    # ssh in (using the remote_key) and restart
    - |
      ssh -i ~/.ssh/remote_key -o StrictHostKeyChecking=no $REMOTE_USER@$REMOTE_HOST <<'EOF'
        set -e

        # Install Docker if not installed
        if ! command -v docker &> /dev/null; then
          echo "Installing Docker and docker compose plugin..."

          sudo apt update
          sudo apt install -y ca-certificates curl gnupg lsb-release

          sudo install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          sudo chmod a+r /etc/apt/keyrings/docker.gpg

          echo \
            "deb [arch=\$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
            https://download.docker.com/linux/ubuntu \$(lsb_release -cs) stable" | \
            sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

          sudo apt update
          sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
        fi

        docker compose version
        cd $REMOTE_PATH
        docker compose up -d --build
      EOF
  rules:
    - if: '$CI_COMMIT_BRANCH =~ /^test\//'
      when: always
